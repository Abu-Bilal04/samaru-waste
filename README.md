# Samaru Waste Management System

A blockchain-based waste management system integated with USSD for offline access and a web dashboard for administration.

## Structure
- `ussd/`: Logic for USSD interactions.
- `api/`: Backend endpoints for data processing and blockchain interaction.
- `dashboard/`: Admin interface.
- `config/`: Configuration files.
- `logs/`: Application logs.
